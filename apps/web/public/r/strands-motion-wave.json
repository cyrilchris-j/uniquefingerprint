{
  "$schema": "https://openui.dev/schema/registry-item.json",
  "name": "strands-motion-wave",
  "type": "registry:component",
  "title": "Strands Motion Wave",
  "description": "Harmonic sine strands oscillating with dynamic wind physics.",
  "category": "motion-design",
  "dependencies": [
    "react"
  ],
  "registryDependencies": [
    "cn"
  ],
  "files": [
    {
      "path": "strands-motion-wave.tsx",
      "type": "registry:component",
      "content": "import * as React from \"react\";\n\nexport function StrandsMotionWave({ className = \"w-full h-48 relative overflow-hidden bg-[#08090f]\" }: { className?: string }): React.JSX.Element {\n  const canvasRef = React.useRef<HTMLCanvasElement>(null);\n  React.useEffect(() => {\n    const canvas = canvasRef.current!;\n    const ctx = canvas.getContext(\"2d\")!;\n    let w = (canvas.width = canvas.parentElement!.clientWidth);\n    let h = (canvas.height = canvas.parentElement!.clientHeight);\n    let t = 0, animId: number;\n    const strands = 20;\n    const draw = () => {\n      ctx.fillStyle = \"rgba(8,9,15,0.15)\"; ctx.fillRect(0,0,w,h);\n      for (let s = 0; s < strands; s++) {\n        const yBase = (s / strands) * h;\n        const hue = 180 + s * 10;\n        ctx.strokeStyle = `hsla(${hue},75%,65%,0.5)`; ctx.lineWidth = 1;\n        ctx.beginPath();\n        for (let x = 0; x <= w; x += 3) {\n          const y = yBase + Math.sin(x/50 + t + s*0.3) * 20 * Math.cos(t*0.5 + s);\n          x === 0 ? ctx.moveTo(x, y) : ctx.lineTo(x, y);\n        }\n        ctx.stroke();\n      }\n      t += 0.025; animId = requestAnimationFrame(draw);\n    };\n    draw();\n    return () => cancelAnimationFrame(animId);\n  }, []);\n  return <div className={className}><canvas ref={canvasRef} className=\"w-full h-full block\" /></div>;\n}\n",
      "contentHash": "7671111062212a971d7ce4592f68e2d689b046d44b1f2ae3a262098fd1094756",
      "sizeBytes": 1296
    }
  ],
  "tags": [
    "motion",
    "animation",
    "interaction"
  ],
  "license": "MIT",
  "url": "https://uniquefingerprint.web.app/r/strands-motion-wave.json"
}