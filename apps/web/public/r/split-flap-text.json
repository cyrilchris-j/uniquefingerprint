{
  "$schema": "https://uniquefingerprint.web.app/schema/registry-item.json",
  "name": "split-flap-text",
  "type": "registry:component",
  "title": "Split Flap Text",
  "description": "Mechanical airport departure board alphanumeric flip animation.",
  "category": "text-animations",
  "dependencies": [
    "react"
  ],
  "registryDependencies": [
    "cn"
  ],
  "files": [
    {
      "path": "split-flap-text.tsx",
      "type": "registry:component",
      "content": "import * as React from \"react\";\n\nexport function SplitFlapText({ text = \"OPENUI\", className = \"\" }: { text?: string; className?: string }): React.JSX.Element {\n  const chars = \"ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789 !#\";\n  const [display, setDisplay] = React.useState(() => text.split(\"\").map(() => \" \"));\n  React.useEffect(() => {\n    const intervals = text.split(\"\").map((target, i) =>\n      setTimeout(() => {\n        let step = 0;\n        const id = setInterval(() => {\n          setDisplay(d => { const n = [...d]; n[i] = step < chars.length - 1 ? chars[step] : target; return n; });\n          if (step++ >= chars.indexOf(target.toUpperCase())) clearInterval(id);\n        }, 40);\n      }, i * 120)\n    );\n    return () => intervals.forEach(clearTimeout);\n  }, [text]);\n  return (\n    <div className={`flex gap-1 ${className}`}>\n      {display.map((c, i) => (\n        <div key={i} className=\"w-10 h-14 bg-zinc-900 border border-zinc-700 rounded-md flex items-center justify-center font-mono text-xl font-bold text-amber-400 select-none shadow-inner\">\n          {c}\n        </div>\n      ))}\n    </div>\n  );\n}\n",
      "contentHash": "251e712b1aa688d1e6d59d77b8152674e820fa82d5a77709595f23c34ff182ec",
      "sizeBytes": 1110
    }
  ],
  "tags": [
    "typography",
    "text",
    "kinetic"
  ],
  "license": "MIT",
  "url": "https://uniquefingerprint.web.app/r/split-flap-text.json"
}